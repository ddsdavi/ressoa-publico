# ===== SEGREDOS — nunca subir =====
.env
.env.local
.env.*.local
scripts/.keys_*
**/.keys_*
*.pem
*.key

# ===== DADOS PESSOAIS (LGPD) — nunca subir =====
# Contém e-mail, telefone e nome de 12 mil pessoas reais.
activecampaign-export/
*.csv

# ===== Gerados =====
node_modules/
dist/
.wrangler/
app/supabase/
supabase/.temp/
app/painel/node_modules/
app/painel/dist/
__pycache__/
*.pyc
.DS_Store
Thumbs.db

# ===== Temporários =====
*.log
/tmp/

# ferramenta local de sanitizacao (guarda os tokens reais para substituir)
scripts/sanitizar_para_github.py

# planilhas de vendas (dados pessoais + financeiros de compradores)
vendas-hotmart/

# notas de planejamento, com dados da operação
blueprint/

# SQL específico de UMA operação (produtos, consertos pontuais)
operacao/

# Sobras de depuração com dado de gente real. Um script meu já gravou um
# fones.txt na raiz por um caminho mal montado — a regra existe para o
# próximo escorregão não virar commit.
fones.txt
*.emails.txt
recorte_*.txt

# Prompt de passagem entre sessões: cita os endereços de teste do Davi e
# fica só na máquina dele. O equivalente sem dado pessoal é docs/09.
PASSAGEM.md
